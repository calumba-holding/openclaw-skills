# 技术选型

## 语言：Go
选 Go 是因为团队熟悉、部署简单、并发性能好。考虑过 Java 但觉得太重，考虑过 Rust 但学习曲线太陡。

## 数据库：PostgreSQL
选 PG 而不是 MySQL 是因为 JSON 字段支持好，事务隔离级别更灵活。订单服务用了 JSONB 存扩展字段。

## 缓存：Redis
标准选择，没什么争议。注意 key 命名带版本号（如 `v2:user:{id}`），发版时避免读到旧结构。

## 消息队列：Kafka
选 Kafka 而不是 RabbitMQ 是因为需要消息持久化和回溯能力。支付回调如果消费失败，可以重新消费。
